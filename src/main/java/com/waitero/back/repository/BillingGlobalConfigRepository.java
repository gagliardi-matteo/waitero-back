package com.waitero.back.repository;

import com.waitero.back.entity.BillingGlobalConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BillingGlobalConfigRepository extends JpaRepository<BillingGlobalConfig, Long> {
}
